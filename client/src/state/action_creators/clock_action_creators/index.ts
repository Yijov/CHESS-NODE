import { default as setStartTime } from "./setStartTime";
import { default as updateTime } from "./updateTime";
import { default as ResetTimer } from "./resetTimer";

const clockActionCreators = { setStartTime, updateTime, ResetTimer };

export default clockActionCreators;
